import { ObjectToCollectionPipe } from './object-to-collection.pipe';

describe('ObjectToCollectionPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectToCollectionPipe();
    expect(pipe).toBeTruthy();
  });
});
