import { DefineCityListPipe } from './define-city-list.pipe';

describe('DefineCityListPipe', () => {
  it('create an instance', () => {
    const pipe = new DefineCityListPipe();
    expect(pipe).toBeTruthy();
  });
});
